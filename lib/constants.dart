import 'package:flutter/material.dart';

const staricon = Icon(
  Icons.star,
  color: Colors.green,
  size: 20,
);

const stariconUnstyled = Icon(
  Icons.star,
  color: Colors.grey,
  size: 20,
);

const favIcon = Icon(
  Icons.favorite,
  color: Colors.green,
  size: 20,
);

const Textmajor =
    TextStyle(color: Colors.green, fontWeight: FontWeight.bold, fontSize: 24);

const TextmajorWhite =
    TextStyle(color: Colors.white, fontWeight: FontWeight.bold, fontSize: 24);

const TextMinorWhite = TextStyle(color: Colors.white, fontSize: 13);

const TextMinor = TextStyle(color: Colors.black, fontSize: 13);

const nameText =
    TextStyle(color: Colors.white, fontWeight: FontWeight.bold, fontSize: 18);
